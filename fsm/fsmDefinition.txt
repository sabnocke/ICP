name TOF5s
# Timer to off.
Input:
    in
Output:
    out
Variables:
    int timeout = 5000
States:
    IDLE [ output("out", 0) ]
    ACTIVE [ output("out", 1) ]
    TIMING []
Transitions:
    IDLE --> ACTIVE: in [ tonumber(in) == 1 ]
    ACTIVE --> TIMING: in [ tonumber(in) == 0 ]
    TIMING --> ACTIVE : in [ tonumber(in) == 1 ]
    TIMING --> IDLE : @ timeout