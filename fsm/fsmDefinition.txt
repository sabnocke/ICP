name TOF5s
# Timer to off.
Input:
    in1
Output:
    out1
Variables:
    int timeout = 5000
States:
state IDLE [ out(1) ]
state ACTIVE [ out(1) ]
state TIMING []
Transitions:
    IDLE --> ACTIVE: in [ tonumber(in1()) == 1 ]
    ACTIVE --> TIMING: in [ tonumber(in1()) == 0 ]
    TIMING --> ACTIVE : in [ tonumber(in1()) == 1 ]
    TIMING --> IDLE : @ timeout